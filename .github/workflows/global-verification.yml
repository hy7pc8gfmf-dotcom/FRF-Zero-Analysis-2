name: FRF 2.0 全局形式化验证
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  formal-verification:
    runs-on: ubuntu-22.04
    timeout-minutes: 60  # 新增模块编译，适当延长超时时间
    
    steps:
    - name: 检出代码仓库
      uses: actions/checkout@v4

    - name: 安装系统依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          opam \
          bubblewrap \
          g++ \
          pkg-config \
          m4 \
          rsync \
          git \
          wget
        echo "✅ 系统依赖安装完成"

    - name: 初始化OPAM环境
      run: |
        opam init --disable-sandboxing -y --compiler=4.14.0
        eval $(opam env)
        opam repo add coq-released https://coq.inria.fr/opam/released
        opam update
        echo "✅ OPAM环境初始化完成"

    - name: 安装兼容的Coq和依赖
      run: |
        eval $(opam env)
        echo "安装Coq 8.18.0..."
        opam install -y coq.8.18.0
        echo "安装兼容的mathcomp-ssreflect..."
        opam install -y coq-mathcomp-ssreflect.1.17.0
        echo "验证安装版本..."
        coqc --version
        opam list --installed | grep -E "coq|mathcomp"
        echo "✅ 兼容环境安装完成"

    - name: 准备工作目录
      run: |
        # 确保所有模块目录存在（含新增CS_Null目录）
        mkdir -p SelfContainedLib theories CS_Null Test
        echo "✅ 工作目录准备完成"

    - name: 编译基础模块（一级核心）
      run: |
        eval $(opam env)
        echo "=== 编译基础模块 ==="
        BASE_FLAGS="-Q SelfContainedLib SelfContainedLib"
        
        # 编译Algebra.v（已修复导入路径）
        echo "编译Algebra.v..."
        if [ -f "SelfContainedLib/Algebra.v" ]; then
          coqc $BASE_FLAGS SelfContainedLib/Algebra.v && echo "✅ Algebra.v 编译成功" || {
            echo "❌ Algebra.v 编译失败"
            coqc $BASE_FLAGS SelfContainedLib/Algebra.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ Algebra.v 文件缺失"
          exit 1
        fi
        
        # 编译Category.v（已修复id_left/id_right参数顺序）
        echo "编译Category.v..."
        if [ -f "SelfContainedLib/Category.v" ]; then
          coqc $BASE_FLAGS SelfContainedLib/Category.v && echo "✅ Category.v 编译成功" || {
            echo "❌ Category.v 编译失败"
            coqc $BASE_FLAGS SelfContainedLib/Category.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ Category.v 文件缺失"
          exit 1
        fi
        
        # 编译Geometry.v（已修复nat作用域和导入路径）
        echo "编译Geometry.v..."
        if [ -f "SelfContainedLib/Geometry.v" ]; then
          coqc $BASE_FLAGS SelfContainedLib/Geometry.v && echo "✅ Geometry.v 编译成功" || {
            echo "❌ Geometry.v 编译失败"
            coqc $BASE_FLAGS SelfContainedLib/Geometry.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ Geometry.v 文件缺失"
          exit 1
        fi

    - name: 编译FRF核心理论（二级元理论）
      run: |
        eval $(opam env)
        echo "=== 编译FRF核心理论 ==="
        BASE_FLAGS="-Q SelfContainedLib SelfContainedLib -Q theories FRF.Theories"
        
        # 编译FRF_MetaTheory.v（已修复FunctionalExtensionality导入）
        echo "编译FRF_MetaTheory.v..."
        if [ -f "theories/FRF_MetaTheory.v" ]; then
          coqc $BASE_FLAGS theories/FRF_MetaTheory.v && echo "✅ FRF_MetaTheory.v 编译成功" || {
            echo "❌ FRF_MetaTheory.v 编译失败"
            coqc $BASE_FLAGS theories/FRF_MetaTheory.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ FRF_MetaTheory.v 文件缺失"
          exit 1
        fi

    - name: 编译Church数值理论（二级元理论）
      run: |
        eval $(opam env)
        echo "=== 编译Church数值理论 ==="
        BASE_FLAGS="-Q SelfContainedLib SelfContainedLib -Q theories FRF.Theories"
        
        # 编译ChurchNumerals.v（已修复λ项定义和归约关系）
        echo "编译ChurchNumerals.v..."
        if [ -f "theories/ChurchNumerals.v" ]; then
          coqc $BASE_FLAGS theories/ChurchNumerals.v && echo "✅ ChurchNumerals.v 编译成功" || {
            echo "❌ ChurchNumerals.v 编译失败"
            coqc $BASE_FLAGS theories/ChurchNumerals.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ ChurchNumerals.v 文件缺失"
          exit 1
        fi
        
        # 编译ChurchZero.v（新增，依赖ChurchNumerals.v）
        echo "编译ChurchZero.v..."
        if [ -f "theories/ChurchZero.v" ]; then
          coqc $BASE_FLAGS theories/ChurchZero.v && echo "✅ ChurchZero.v 编译成功" || {
            echo "❌ ChurchZero.v 编译失败"
            coqc $BASE_FLAGS theories/ChurchZero.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ ChurchZero.v 文件缺失"
          exit 1
        fi

    - name: 编译CS空值通用模块（三级扩展）
      run: |
        eval $(opam env)
        echo "=== 编译CS空值通用模块 ==="
        # 路径映射包含基础库、元理论、CS_Null目录
        BASE_FLAGS="-Q SelfContainedLib SelfContainedLib -Q theories FRF.Theories -Q CS_Null FRF.CS_Null"
        
        # 编译FRF_CS_Null_Common.v（已修复Mathlib导入和类型适配）
        echo "编译FRF_CS_Null_Common.v..."
        if [ -f "CS_Null/FRF_CS_Null_Common.v" ]; then
          coqc $BASE_FLAGS CS_Null/FRF_CS_Null_Common.v && echo "✅ FRF_CS_Null_Common.v 编译成功" || {
            echo "❌ FRF_CS_Null_Common.v 编译失败"
            coqc $BASE_FLAGS CS_Null/FRF_CS_Null_Common.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ FRF_CS_Null_Common.v 文件缺失"
          exit 1
        fi

    - name: 编译测试模块（验证基础功能）
      run: |
        eval $(opam env)
        echo "=== 编译测试模块 ==="
        BASE_FLAGS="-Q SelfContainedLib SelfContainedLib -Q theories FRF.Theories -Q CS_Null FRF.CS_Null -Q Test FRF.Test"
        
        # 编译Test_Basic.v（基础逻辑测试）
        echo "编译Test_Basic.v..."
        if [ -f "Test/Test_Basic.v" ]; then
          coqc $BASE_FLAGS Test/Test_Basic.v && echo "✅ Test_Basic.v 编译成功" || {
            echo "❌ Test_Basic.v 编译失败"
            coqc $BASE_FLAGS Test/Test_Basic.v 2>&1 | head -10
            exit 1
          }
        else
          echo "❌ Test_Basic.v 文件缺失"
          exit 1
        fi

    - name: 编译结果分析
      if: always()
      run: |
        eval $(opam env)
        echo "=== 编译结果分析 ==="
        total_v_files=$(find . -name "*.v" -path "./SelfContainedLib/*" -o -path "./theories/*" -o -path "./CS_Null/*" -o -path "./Test/*" | wc -l)
        compiled_vo_files=$(find . -name "*.vo" -path "./SelfContainedLib/*" -o -path "./theories/*" -o -path "./CS_Null/*" -o -path "./Test/*" | wc -l)
        echo "统计信息:"
        echo "- 核心模块总数: $total_v_files"
        echo "- 成功编译数: $compiled_vo_files"
        
        # 定义所有核心模块列表（含新增模块）
        modules=("Algebra" "Category" "Geometry" "FRF_MetaTheory" "ChurchNumerals" "ChurchZero" "FRF_CS_Null_Common" "Test_Basic")
        
        if [ $compiled_vo_files -gt 0 ]; then
          echo "✅ 编译成功!"
          echo "已编译模块:"
          for module in "${modules[@]}"; do
            if find . -name "${module}.vo" | grep -q .; then
              echo "  ✅ $module"
            else
              echo "  ❌ $module"
            fi
          done
          success_rate=$((compiled_vo_files * 100 / total_v_files))
          echo "编译成功率: $success_rate%"
        else
          echo "❌ 编译失败"
          exit 1
        fi

    - name: 生成验证报告
      if: always()
      run: |
        eval $(opam env)
        echo "=== 生成验证报告 ==="
        mkdir -p verification-reports
        total_v_files=$(find . -name "*.v" -path "./SelfContainedLib/*" -o -path "./theories/*" -o -path "./CS_Null/*" -o -path "./Test/*" | wc -l)
        compiled_vo_files=$(find . -name "*.vo" -path "./SelfContainedLib/*" -o -path "./theories/*" -o -path "./CS_Null/*" -o -path "./Test/*" | wc -l)
        coq_version=$(coqc --version | head -1)
        
        echo "# FRF 2.0 形式化验证报告" > verification-reports/REPORT.md
        echo "" >> verification-reports/REPORT.md
        echo "**验证时间**: $(date)" >> verification-reports/REPORT.md
        echo "**环境**: Ubuntu 22.04" >> verification-reports/REPORT.md
        echo "**Coq版本**: $coq_version" >> verification-reports/REPORT.md
        echo "" >> verification-reports/REPORT.md
        
        echo "## 编译统计" >> verification-reports/REPORT.md
        echo "- 核心模块总数: $total_v_files" >> verification-reports/REPORT.md
        echo "- 成功编译数: $compiled_vo_files" >> verification-reports/REPORT.md
        if [ $total_v_files -gt 0 ]; then
          success_rate=$((compiled_vo_files * 100 / total_v_files))
          echo "- 编译成功率: $success_rate%" >> verification-reports/REPORT.md
        fi
        
        echo "" >> verification-reports/REPORT.md
        echo "## 模块状态详情" >> verification-reports/REPORT.md
        echo "| 模块 | 层级 | 状态 | 核心功能 |" >> verification-reports/REPORT.md
        echo "|------|------|------|----------|" >> verification-reports/REPORT.md
        
        # 模块详情表格（含层级和功能说明）
        module_details=(
          "Algebra|一级基础|$(if find . -name "Algebra.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|代数结构（幺半群/群）定义与公理"
          "Category|一级基础|$(if find . -name "Category.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|范畴论核心（预范畴/函子/零对象）"
          "Geometry|一级基础|$(if find . -name "Geometry.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|几何流形（球面/双曲）与度规"
          "FRF_MetaTheory|二级元理论|$(if find . -name "FRF_MetaTheory.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|FRF框架元理论（形式系统/功能角色）"
          "ChurchNumerals|二级元理论|$(if find . -name "ChurchNumerals.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|λ演算Church数定义与归约"
          "ChurchZero|二级元理论|$(if find . -name "ChurchZero.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|Church零的功能唯一性验证"
          "FRF_CS_Null_Common|三级扩展|$(if find . -name "FRF_CS_Null_Common.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|跨语言空值FRF通用基础"
          "Test_Basic|测试模块|$(if find . -name "Test_Basic.vo" | grep -q .; then echo "✅ 成功"; else echo "❌ 失败"; fi)|基础逻辑与算术验证"
        )
        
        for detail in "${module_details[@]}"; do
          echo "| $(echo $detail | tr '|' '|') |" >> verification-reports/REPORT.md
        done
        
        echo "" >> verification-reports/REPORT.md
        echo "## 环境信息" >> verification-reports/REPORT.md
        echo "\`\`\`" >> verification-reports/REPORT.md
        opam list --installed | grep -E "coq|mathcomp" >> verification-reports/REPORT.md 2>/dev/null || echo "无法获取包信息" >> verification-reports/REPORT.md
        echo "\`\`\`" >> verification-reports/REPORT.md
        
        echo "✅ 验证报告生成完成"

    - name: 上传验证报告
      uses: actions/upload-artifact@v4
      with:
        name: frf-verification-report
        path: verification-reports/
        retention-days: 7

    - name: 验证状态总结
      if: always()
      run: |
        eval $(opam env)
        compiled_count=$(find . -name "*.vo" -path "./SelfContainedLib/*" -o -path "./theories/*" -o -path "./CS_Null/*" -o -path "./Test/*" | wc -l)
        total_core_modules=8
        
        if [ $compiled_count -eq $total_core_modules ]; then
          echo "🎉 FRF 2.0 验证完全成功!"
          echo "📊 所有核心模块（$compiled_count/$total_core_modules）编译通过"
          echo "✅ 基础层+元理论层+扩展层+测试层全量验证通过"
        elif [ $compiled_count -ge 6 ]; then
          echo "🎉 FRF 2.0 验证基本成功!"
          echo "📊 编译模块数：$compiled_count/$total_core_modules"
          echo "✅ 核心框架（基础层+元理论层）验证通过"
        elif [ $compiled_count -ge 3 ]; then
          echo "⚠️ FRF 2.0 验证部分成功"
          echo "📊 编译模块数：$compiled_count/$total_core_modules"
          echo "🔧 基础层验证通过，需优化元理论/扩展层模块"
          exit 1
        else
          echo "❌ FRF 2.0 验证失败"
          echo "📋 详细报告已生成，需优先修复基础模块"
          exit 1
        fi