name: FRF 2.0 全局形式化验证
on:
  push:
    branches: [ main, master, release/final ]
    paths: [ '**.v', 'CoqProject', '_CoqProject', 'Makefile', 'validate.sh' ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:
jobs:
  global-formal-verification:
    runs-on: ubuntu-latest
    container:
      image: coqorg/coq:8.18.0
      options: --user root
    timeout-minutes: 180
    env:
      MATHCOMP_VERSION: "1.18.0"
      COQ_VERSION: "8.18.0"
      COQ_EXTRA_OPAM: "coq-bignums"
      COQPATH: "${{ github.workspace }}/theories:${{ github.workspace }}/lib:${{ github.workspace }}/extensions"
      GLOBAL_TARGET: "all"
      REPORT_DIR: "${{ github.workspace }}/verification-reports"
      TEMP_DIR: "/__w/_temp/_runner_file_commands"
      OPAM_MAIN_MIRROR: "https://mirrors.tuna.tsinghua.edu.cn/opam-repository/"
      COQ_MAIN_MIRROR: "https://mirrors.tuna.tsinghua.edu.cn/coq-opam/released/"
      OPAM_FALLBACK_MIRROR: "https://opam.ocaml.org/"
      COQ_FALLBACK_MIRROR: "https://coq.inria.fr/opam/released/"
      VENV_PATH: "${{ github.workspace }}/.venv"
      COQ_LOGICAL_ROOT: "FRF"
    steps:
      - name: 修复临时目录权限
        run: |
          mkdir -p ${{ env.TEMP_DIR }} ~/.opam ~/.cache/opam
          chmod -R 777 /__w/_temp/ ~/.opam ~/.cache/opam
          echo "✅ 临时目录权限修复完成"
      - name: 检出代码仓库
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          submodules: 'recursive'
      - name: 修复工作目录权限+初始化opam环境
        run: |
          mkdir -p ${{ github.workspace }}
          chmod -R 777 ${{ github.workspace }}
          opam init --disable-sandboxing -y --no-setup --reinit
          eval $(opam env --switch=default)
          echo "✅ 目录权限修复+opam环境初始化完成"
          echo "📌 当前opam版本：$(opam --version)"
          if command -v coqc &> /dev/null; then
            echo "📌 当前Coq版本：$(coqc --version | head -n1)"
          else
            echo "⚠️  初始Coq未找到，将通过opam安装"
          fi
      - name: 缓存优化
        uses: actions/cache@v3
        with:
          path: |
            ~/.opam
            ~/.cache/opam
            _build
            **/*.vo
            **/*.glob
            ${{ env.VENV_PATH }}
          key: ${{ runner.os }}-coq-${{ env.COQ_VERSION }}-mathcomp-${{ env.MATHCOMP_VERSION }}-venv-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-coq-${{ env.COQ_VERSION }}-mathcomp-${{ env.MATHCOMP_VERSION }}-venv-
            ${{ runner.os }}-coq-${{ env.COQ_VERSION }}-venv-
      - name: 配置镜像源+安装依赖
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          opam repo list --all-switches
          if curl --head --silent --fail ${{ env.OPAM_MAIN_MIRROR }}index.tar.gz > /dev/null; then
            opam repository set-url default ${{ env.OPAM_MAIN_MIRROR }} --all-switches || {
              opam repo remove default --all-switches || true
              opam repo add default ${{ env.OPAM_MAIN_MIRROR }} --all-switches
            }
          else
            opam repository set-url default ${{ env.OPAM_FALLBACK_MIRROR }} --all-switches || {
              opam repo remove default --all-switches || true
              opam repo add default ${{ env.OPAM_FALLBACK_MIRROR }} --all-switches
            }
          fi
          if curl --head --silent --fail ${{ env.COQ_MAIN_MIRROR }}index.tar.gz > /dev/null; then
            if opam repo list | grep -q "coq-released"; then
              opam repository set-url coq-released ${{ env.COQ_MAIN_MIRROR }} --all-switches
            else
              opam repo add coq-released ${{ env.COQ_MAIN_MIRROR }} --all-switches
            fi
          else
            if opam repo list | grep -q "coq-released"; then
              opam repository set-url coq-released ${{ env.COQ_FALLBACK_MIRROR }} --all-switches
            else
              opam repo add coq-released ${{ env.COQ_FALLBACK_MIRROR }} --all-switches
            fi
          fi
          update_success=0
          for i in {1..3}; do
            echo "=== 第 $i 次尝试更新仓库 ==="
            if opam update -y; then
              update_success=1
              break
            fi
            sleep 2
          done
          if [ $update_success -eq 0 ]; then
            opam repository set-url default ${{ env.OPAM_FALLBACK_MIRROR }} --all-switches
            opam repository set-url coq-released ${{ env.COQ_FALLBACK_MIRROR }} --all-switches
            for i in {1..3}; do
              echo "=== 第 $i 次尝试兜底镜像更新 ==="
              if opam update -y; then
                update_success=1
                break
              fi
              sleep 2
            done
          fi
          if [ $update_success -eq 0 ]; then
            echo "❌ 所有更新尝试均失败"
            exit 1
          fi
          installed_pkgs=$(opam list --short)
          required_pkgs=(
            "coq=${{ env.COQ_VERSION }}"
            "coq-stdlib=${{ env.COQ_VERSION }}"
            "coq-mathcomp-ssreflect=${{ env.MATHCOMP_VERSION }}"
            "coq-mathcomp-fingroup=${{ env.MATHCOMP_VERSION }}"
            "coq-mathcomp-algebra=${{ env.MATHCOMP_VERSION }}"
            "coq-mathcomp-solvable=${{ env.MATHCOMP_VERSION }}"
            "coq-equations=1.3+8.18"
            "${{ env.COQ_EXTRA_OPAM }}"
          )
          to_install=()
          for pkg in "${required_pkgs[@]}"; do
            pkg_name=$(echo "$pkg" | cut -d'=' -f1)
            if ! echo "$installed_pkgs" | grep -q "^$pkg_name$"; then
              to_install+=("$pkg")
              echo "📥 待安装：$pkg"
            else
              echo "✅ 已安装：$pkg，跳过"
            fi
          done
          if [ ${#to_install[@]} -gt 0 ]; then
            install_success=0
            for i in {1..3}; do
              echo "=== 第 $i 次尝试安装依赖 ==="
              if opam install -y --jobs 2 "${to_install[@]}"; then
                install_success=1
                break
              fi
              sleep 5
            done
            if [ $install_success -eq 0 ]; then
              echo "❌ 所有依赖安装尝试均失败"
              exit 1
            fi
          else
            echo "=== ✅ 所有依赖已完整安装，无需额外操作 ==="
          fi
          eval $(opam env --switch=default)
          apt-get update && apt-get install -y --no-install-recommends python3-pip python3-venv && rm -rf /var/lib/apt/lists/*
          python3 -m venv ${{ env.VENV_PATH }}
          source ${{ env.VENV_PATH }}/bin/activate
          pip install --no-cache-dir --timeout 60 reportlab==4.0.9
          if ! command -v coqc &> /dev/null; then
            echo "❌ Coq未安装成功"
            exit 1
          fi
          if ! coqc --version | grep -q "${{ env.COQ_VERSION }}"; then
            echo "❌ Coq版本不匹配（需${{ env.COQ_VERSION }}，当前：$(coqc --version | head -n1)）"
            exit 1
          fi
          if ! opam list | grep -q "coq-mathcomp-ssreflect.*${{ env.MATHCOMP_VERSION }}"; then
            echo "❌ MathComp版本不匹配"
            exit 1
          fi
          if ! command -v python &> /dev/null; then
            echo "❌ Python虚拟环境激活失败"
            exit 1
          fi
          echo "=== ✅ 依赖安装+校验全量完成，准备进入验证阶段 ==="
      - name: 校验并修复CoqProject配置
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          source ${{ env.VENV_PATH }}/bin/activate
          COQ_PROJECT_FILE="CoqProject"
          if [ -f "_CoqProject" ]; then
            COQ_PROJECT_FILE="_CoqProject"
            echo "=== 发现 _CoqProject，使用该文件作为配置 ==="
          fi
          if [ ! -f "$COQ_PROJECT_FILE" ]; then
            echo "=== 未找到CoqProject文件，创建默认配置 ==="
            cat > "$COQ_PROJECT_FILE" <<EOF
-R theories ${{ env.COQ_LOGICAL_ROOT }}.Theories
-R lib ${{ env.COQ_LOGICAL_ROOT }}.Lib
-R extensions ${{ env.COQ_LOGICAL_ROOT }}.Extensions
-arg -w -arg -notation-overridden
-arg -native-compiler -arg -noinit
EOF
            for vfile in $(find theories lib extensions -name "*.v" -type f | head -50); do
              echo "$vfile" >> "$COQ_PROJECT_FILE"
            done
          else
            echo "=== 修复CoqProject路径映射 ==="
            if ! grep -q "^-R" "$COQ_PROJECT_FILE" && ! grep -q "^-Q" "$COQ_PROJECT_FILE"; then
              sed -i "1i-R theories ${{ env.COQ_LOGICAL_ROOT }}.Theories" "$COQ_PROJECT_FILE"
              sed -i "2i-R lib ${{ env.COQ_LOGICAL_ROOT }}.Lib" "$COQ_PROJECT_FILE"
              sed -i "3i-R extensions ${{ env.COQ_LOGICAL_ROOT }}.Extensions" "$COQ_PROJECT_FILE"
            fi
            if ! grep -q "-arg -w" "$COQ_PROJECT_FILE"; then
              echo "-arg -w -arg -notation-overridden" >> "$COQ_PROJECT_FILE"
            fi
          fi
          echo "=== 最终CoqProject配置（前20行） ==="
          head -n 20 "$COQ_PROJECT_FILE"
          echo "=== ✅ CoqProject配置完成 ==="
      - name: 生成全局验证Makefile
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          source ${{ env.VENV_PATH }}/bin/activate
          COQ_PROJECT_FILE="CoqProject"
          [ -f "_CoqProject" ] && COQ_PROJECT_FILE="_CoqProject"
          if [ -f "Makefile.template" ]; then
            coq_makefile -f "$COQ_PROJECT_FILE" -o Makefile -extra Makefile.template
          else
            coq_makefile -f "$COQ_PROJECT_FILE" -o Makefile
          fi
          if [ ! -f "Makefile" ]; then
            echo "❌ Makefile生成失败，未找到输出文件"
            exit 1
          fi
          if ! grep -q "${{ env.GLOBAL_TARGET }}" Makefile; then
            echo "all: Makefile.coq" >> Makefile
            echo "	\$(MAKE) -f Makefile.coq all" >> Makefile
          fi
          echo "✅ Makefile生成完成，验证目标就绪"
          echo "=== 编译命令示例（前10行） ==="
          grep -E "^[a-zA-Z0-9_]+:.*coqc" Makefile | head -n 10
      - name: 执行全局形式化验证
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          source ${{ env.VENV_PATH }}/bin/activate
          mkdir -p ${{ env.REPORT_DIR }}
          echo "=== 🎯 开始全局形式化验证（$(nproc)核并行编译） ==="
          make -j$(nproc) ${{ env.GLOBAL_TARGET }} VERBOSE=1 2>&1 | tee ${{ env.REPORT_DIR }}/global-compile.log
          echo "=== 📝 验证编译产物完整性 ==="
          COQ_PROJECT_FILE="CoqProject"
          [ -f "_CoqProject" ] && COQ_PROJECT_FILE="_CoqProject"
          failed_files=()
          while IFS= read -r v_file; do
            [ -z "$v_file" ] && continue
            [[ "$v_file" == -* ]] && continue
            vo_file="${v_file%.v}.vo"
            if [ ! -f "$vo_file" ]; then
              failed_files+=("$vo_file")
            fi
          done < <(grep -E '^[^#-].*\.v$' "$COQ_PROJECT_FILE")
          if [ ${#failed_files[@]} -ne 0 ]; then
            echo "❌ 以下文件验证失败（未生成.vo产物）："
            printf '%s\n' "${failed_files[@]}"
            exit 1
          fi
          echo "✅ 所有文件编译验证完成，无缺失产物"
      - name: 全局逻辑一致性校验
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          source ${{ env.VENV_PATH }}/bin/activate
          echo "=== 🧐 执行全局逻辑一致性检查 ==="
          coqchk -silent -norecursive -Q theories ${{ env.COQ_LOGICAL_ROOT }}.Theories -Q lib ${{ env.COQ_LOGICAL_ROOT }}.Lib -Q extensions ${{ env.COQ_LOGICAL_ROOT }}.Extensions $(find theories/ lib/ extensions/ -name "*.vo") 2>&1 | tee ${{ env.REPORT_DIR }}/global-coqchk.log
          if [ ${PIPESTATUS[0]} -ne 0 ]; then
            echo "❌ 全局逻辑一致性检查失败，存在逻辑矛盾或依赖缺失"
            exit 1
          fi
          echo "✅ 全局逻辑一致性校验通过，无逻辑错误"
      - name: 生成结构化验证报告
        shell: bash
        working-directory: ${{ github.workspace }}
        run: |
          eval $(opam env --switch=default)
          source ${{ env.VENV_PATH }}/bin/activate
          COQ_PROJECT_FILE="CoqProject"
          [ -f "_CoqProject" ] && COQ_PROJECT_FILE="_CoqProject"
          total_files=$(grep -E '^[^#-].*\.v$' "$COQ_PROJECT_FILE" | wc -l)
          success_files=$(find theories/ lib/ extensions/ -name "*.vo" | wc -l)
          cat > ${{ env.REPORT_DIR }}/SUMMARY.md <<EOF
          # FRF 2.0 全局形式化验证报告（最终版）
          
          ## 🔧 验证环境信息
          - 验证时间: $(date +'%Y-%m-%d %H:%M:%S')
          - 触发事件: ${{ github.event_name }}
          - 提交哈希: ${{ github.sha }}
          - 运行容器: coqorg/coq:${{ env.COQ_VERSION }}
          - Coq版本: $(coqc --version | head -n1)
          - MathComp版本: ${{ env.MATHCOMP_VERSION }}
          - 编译核心数: $(nproc)
          - 逻辑根路径: ${{ env.COQ_LOGICAL_ROOT }}
          
          ## 📊 验证范围统计
          - 总待验证文件数: $total_files
          - 成功验证文件数: $success_files
          - 逻辑一致性: ✅ 校验通过
          
          ## 🎯 核心模块验证状态
          EOF
          core_modules=(
            "theories/FRF_MetaTheory.vo"
            "theories/Equivalence.vo"
            "lib/Algebra.vo"
            "extensions/Quantum/QFT_FRF.vo"
          )
          for mod in "${core_modules[@]}"; do
            if [ -f "$mod" ]; then
              echo "✅ 核心模块: $mod" >> ${{ env.REPORT_DIR }}/SUMMARY.md
            else
              echo "❌ 核心模块缺失: $mod" >> ${{ env.REPORT_DIR }}/SUMMARY.md
            fi
          done
          echo -e "\n## 📋 详细日志" >> ${{ env.REPORT_DIR }}/SUMMARY.md
          echo "- [编译日志](global-compile.log)" >> ${{ env.REPORT_DIR }}/SUMMARY.md
          echo "- [逻辑一致性检查日志](global-coqchk.log)" >> ${{ env.REPORT_DIR }}/SUMMARY.md
          echo "- [CoqProject配置]($COQ_PROJECT_FILE)" >> ${{ env.REPORT_DIR }}/SUMMARY.md
          echo "✅ 结构化验证报告生成完成"
      - name: 归档验证产物与报告
        uses: actions/upload-artifact@v4
        with:
          name: frf-2.0-verification-results-${{ github.sha }}
          path: |
            ${{ env.REPORT_DIR }}/**/*
            **/*.vo
            **/*.glob
            Makefile
            CoqProject
            _CoqProject
            ${{ env.VENV_PATH }}/lib/python*/site-packages/*.dist-info
          retention-days: 90
      - name: 验证失败紧急通知
        if: failure()
        run: |
          echo "❌ FRF 2.0 全局形式化验证失败"
          echo "📌 失败原因：Coq编译参数缺失或路径配置错误"
          echo "📌 请查看归档产物中的CoqProject配置和编译日志"